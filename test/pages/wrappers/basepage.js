const Page = require("./page");
const SideNav = require("../component/sideNav.component");

/**
 * Base page class holds all the common elements and methods that are globally available across
 * different pages in the campaign website.
 *
 * @type {BasePage}
 */
module.exports = class BasePage extends Page {


}